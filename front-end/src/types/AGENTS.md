# Types Guide

Use this folder for shared TypeScript interfaces.

Current use:

- stock search types
- quote types
- watchlist item types

Keep types UI-agnostic so they work across stores, services, and views.
