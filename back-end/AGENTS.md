# Backend Guide

This folder is not part of the current submission flow.

Only use it if the frontend later needs:

- an API proxy
- API key protection
- a workaround for browser-only deployment limits

For this project, prefer keeping the solution frontend-only.
